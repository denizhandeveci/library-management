spring.application.name=library-management

spring.datasource.url=jdbc:mysql://localhost:3306/library_management
spring.datasource.username=root
spring.datasource.password=4815162342bd.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
# Show SQL in the console
# this was really noisy in the console, so turned it off, yb
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# more detailed logs on debug-level
logging.level.com.example.library.management=DEBUG
#logging.level.com.example.library.management=INFO

app.jwt.secret=replace-this-with-a-long-secret-key-at-least-32-characters-long
app.jwt.expiration-minutes=120

app.jwt.cookie.name=access_token
# TODO come up with proper localdev / PROD separated app.properties?, yb
# set to true for prod
app.jwt.cookie.secure=false
app.jwt.cookie.same-site=Strict